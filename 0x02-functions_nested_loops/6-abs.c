#include "main.h"
/**
 * _abs - function to find the absolute value of a number
 * @n: int to check
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
